/**
 * GUI analyzer package. Contains aspects that weave into the target application,
 * handlers for the extraction and record/replay function, experimental statistics
 * and utility classes.
 */
package gui.analyzer;
